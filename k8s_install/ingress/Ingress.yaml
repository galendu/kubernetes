apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: name-ingress
spec:
  rules: 
  - host: web.foo.com
  http:
    paths: 
    - backend:
      serviceName: servicename
      servicePort: 80
